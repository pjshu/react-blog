import {makeStyles} from "@material-ui/core";

export default makeStyles({
  root: {
    width: 345,
    height: '100%',
    maxHeight: 280,
    // minWidth:300,
    // maxWidth: 345,
  },
  media: {
    height: 180,
  },
});
